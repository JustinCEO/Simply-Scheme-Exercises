#lang planet dyoo/simply-scheme:2:2

(define (vowel? letter)
  (member? letter 'aeiou))

(define (square number)
  (* number number))


(define (g b)
  (every b '(blue jay way)))

(define (h c d)
  (c (c d)))

(define (i e)
  (/ (accumulate + e) (count e)))
